using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class Robot3AI : MonoBehaviour
{
    /*// public Vector3 currentPos = posStart;

    [SerializeField]
    Transform destinationStart; //target
    [SerializeField]
    Transform destination1;
    [SerializeField]
    Transform destination2;
    [SerializeField]
    Transform destination3;
    [SerializeField]
    Transform destination4;
    [SerializeField]
    Transform destination5;
    [SerializeField]
    Transform destination6;
    [SerializeField]
    Transform destination7;
    [SerializeField]
    Transform destination8;
    [SerializeField]
    Transform destination9;
    [SerializeField]
    Transform destination10;

    [SerializeField]
    Transform robotPosition;

    Vector3 destination;

    NavMeshAgent navmeshAgent;



    void Start()
    {
        navmeshAgent = this.GetComponent<NavMeshAgent>();
        destination = navmeshAgent.destination;
    }

    /*   private void setDestination() {
           if (destinationStart != null)
           {

               Vector3 robotPos = robotPosition.transform.position;

               Vector3 posStart = destinationStart.transform.position;
               Vector3 pos1 = destination1.transform.position;
               Vector3 pos2 = destination2.transform.position;
               Vector3 pos3 = destination3.transform.position;
               Vector3 pos4 = destination4.transform.position;
               Vector3 pos5 = destination5.transform.position;
               Vector3 pos6 = destination6.transform.position;
               Vector3 pos7 = destination7.transform.position;
               Vector3 pos8 = destination8.transform.position;
               Vector3 pos9 = destination9.transform.position;
               Vector3 pos10 = destination10.transform.position;



                 navmeshAgent.SetDestination(posStart);


           }



       }






    void Update()
    {

        if (Vector3.Distance(destination, destinationStart.position) > 1.0f)
        {
            destination = destinationStart.position;
            navmeshAgent.destination = destination;

        }

        if (navmeshAgent.destination == destination) {
            destination == destination1; 
        }
    }*/
}

    	







                    
        
           
      
        
   

  

   

